<h1>My bookings</h1>

<ul>
  <% @bookings.each do |booking| %>
    <li>
      <%= booking.van.title %> Price: <%= booking.van.price_per_day %>
      <%= link_to "More Info", booking_path(booking)  %>
      <%= link_to "Cancel Booking", delete_booking_path(booking), method: :delete   %>
    </li>
  <% end %>
</ul>
