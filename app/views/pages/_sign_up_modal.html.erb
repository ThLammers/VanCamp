<div class="modal fade" id="signUpModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div id="sign-up-modal" class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content vancamp-modal">
      <!-- HEADER (in case someone needs one just outcomment it)
        <div class="modal-header">
          <h1>Sign Up</h1>
        </div>
      -->
      <div class="modal-body">
        <%= render 'devise/registrations/sign_up_form' %>
      </div>
      <!-- FOOTER (in case someone needs one just outcomment it)
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
      -->
    </div>
  </div>
</div>
