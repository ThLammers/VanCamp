<div class="modal fade" id="confirmBookingModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <!-- HEADER (in case someone needs one just outcomment it)
        <div class="modal-header">
          <h1>Sign Up</h1>
        </div>
      -->
      <div class="modal-body">
        <%= render 'bookings/new' %>
      </div>
      <!-- FOOTER (in case someone needs one just outcomment it)
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
      -->
    </div>
  </div>
</div>
